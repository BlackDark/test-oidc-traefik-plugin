# The name of your plugin as displayed in the Plugins Catalog web UI.
displayName: OIDC Authentication (hardened)

# For now, `middleware` is the only type available.
type: middleware

# The import path of your plugin.
import: github.com/BlackDark/test-oidc-traefik-plugin/src

# A brief description of what your plugin is doing.
summary: "Hardened fork of traefik-oidc-auth: sealed OIDC state, PKCE-in-state, login CSRF, nonce, safer defaults."

# Medias associated to the plugin (optional)
iconPath: .assets/icon.png
# bannerPath: .assets/banner.png

# Configuration data for your plugin.
# This is mandatory, and Plugins Catalog will try to execute the plugin with the data you provide as part of its startup validity tests.
testData:
  secret: "0123456789abcdef0123456789abcdef"
  provider:
    url: "https://..."
    clientId: "<YourClientId>"
    clientSecret: "<YourClientSecret>"
  scopes: ["openid", "profile", "email"]
