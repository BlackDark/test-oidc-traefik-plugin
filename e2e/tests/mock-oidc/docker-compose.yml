services:
  mock-oauth2-server:
    image: ghcr.io/navikt/mock-oauth2-server:5.0.2
    ports:
      - "8000:8080"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./login.html:/app/login.html:ro
    environment:
      SERVER_PORT: "8080"
      JSON_CONFIG_PATH: /app/config.json
      LOG_LEVEL: info
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:8080/isalive"]
      interval: 2s
      timeout: 2s
      retries: 15
      start_period: 3s

  traefik:
    image: traefik:v3.5.0
    ports:
      - "9080:80"
      - "9443:443"
      - "8080:8080"
    extra_hosts:
      - "localhost:172.17.0.1"
      - "127-0-0-1.sslip.io:172.17.0.1"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "../../../workspaces/configs/traefik-config.yml:/etc/traefik/traefik.yml:ro"
      - "../../..:/plugins-local/src/github.com/BlackDark/test-oidc-traefik-plugin:ro"
      - "../../.http.yml:/etc/traefik/configs/http.yml:ro"
      - "./data:/data:ro"
    environment:
      PROVIDER_URL: http://localhost:8000/default
      CLIENT_ID: traefik
      CLIENT_SECRET: test-secret
      TOA_ENABLE_REDIRECT_URI_WILDCARDS: "true"
    depends_on:
      mock-oauth2-server:
        condition: service_healthy

  whoami:
    image: traefik/whoami:latest
